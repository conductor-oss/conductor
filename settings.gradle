rootProject.name='conductor'
include 'common', 'core', 'redis-persistence','es2-persistence', 'es5-persistence','mysql-persistence','jersey', 'client', 'test-harness', 'ui', 'contribs', 'server'
rootProject.children.each {it.name="conductor-${it.name}"}
