dependencies {
    compile project(':conductor-common')
    compile "com.github.vmg.protogen:protogen-codegen:${revProtoGen}"
}

task generate(type: JavaExec) {
    classpath = sourceSets.main.runtimeClasspath
    main = "com.netflix.conductor.protogen.ConductorProtoGen"
    workingDir = rootDir
}
