import { ReactNode } from "react";
export declare const ConductorEmptyGroupField: ({ addButtonLabel, handleAddItem, id, loading, compact, emptyListMessage, }: {
    addButtonLabel?: ReactNode;
    handleAddItem: () => void;
    id?: string;
    loading?: boolean;
    compact?: boolean;
    emptyListMessage?: ReactNode;
}) => import("react").JSX.Element;
