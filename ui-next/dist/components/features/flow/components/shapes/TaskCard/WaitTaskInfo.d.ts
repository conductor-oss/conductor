import { WaitTaskDef } from "types";
interface WaitTaskInfoProps {
    task: WaitTaskDef;
}
export declare const WaitTaskInfo: ({ task }: WaitTaskInfoProps) => import("react").JSX.Element | null;
export {};
