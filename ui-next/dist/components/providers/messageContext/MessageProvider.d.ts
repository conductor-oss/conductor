import { ReactNode } from "react";
interface MessageProviderProps {
    children?: ReactNode;
}
export declare const MessageProvider: ({ children }: MessageProviderProps) => import("react").JSX.Element;
export {};
